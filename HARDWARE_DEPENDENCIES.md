# 待定硬件依赖清单

> 软件负责人用。列出「硬件/传感器尚未确定」而阻塞软件落地的所有项。
> 优先级：**P0 = 阻塞核心链路，需最先催**；P1 = 重要但可并行；P2 = 可后补。

---

## 1. 转运机构方案（P0 · 规则符合性风险）

**待定**：实际转运机构是「套筒从上往下套住 + 推着走」，还是「夹爪抓起放上车」？

**阻塞的软件**：`transport/` 整个模块（`sleeve_lift.py` 的 RAISED/LOWERED/HOLD 抽象、`transport_pipeline.py` 的套取/投放流程）。

**为什么是 P0**：
- 赛规明确「禁止抓取救援目标或将其放置在机器人上，只能**推/拨/铲**」。
- 若机构是「套筒罩住后贴着地面推」，软件抽象能适配（合规）；若是「抓取」，则整体方案违规。
- **必须先和机械负责人确认实际机构**，否则 `transport` 写对了方向也可能白做。

**需要确认**：机构类型、升降行程(mm)、驱动方式（丝杆电机/舵机）、是否有上下限位、能否套住 40~80mm 的目标。

---

## 2. 摄像头 + 内参标定（P0 · 感知定位核心）

**待定**：摄像头型号、安装高度/俯仰角、分辨率、FOV、内参。

**阻塞的软件**：
- `perception/detection.py` 的 `CVDetector.estimate_position`（针孔模型，需要 `camera_height_mm`/`camera_fov_deg`/`image_size`）。
- 目标的**场地坐标定位**（cm 级精度）——这是整套感知的根基。

**需要确认**：型号、分辨率、帧率（≥10fps）、安装位置与角度、内参标定数据。

---

## 3. IMU + 定位方案（P0 · 定位核心）

**待定**：IMU 型号、接口（I2C/SPI）、是否加视觉/激光 SLAM。

**阻塞的软件**：
- `navigation/localization.py` 的 `OdometryLocalizer`（里程计+IMU 互补滤波）vs `VisualSLAMLocalizer`（stub）。
- 3m×3m 场地 cm 级定位，纯里程计漂移风险高，需确定「里程计+IMU」是否够，还是必须上视觉/激光。

**需要确认**：IMU 型号/量程/采样率、接口、是否有激光雷达或依赖视觉 SLAM。

---

## 4. 电机 + 编码器（P0 · 真实底盘）

**待定**：电机型号/数量（2 差速？4 全向？）、驱动方式（PWM/串口）、编码器线数、减速比。

**阻塞的软件**：
- `config.py` 的 `MOTOR_REDUCTION`/`ENCODER_PPR` 等占位参数。
- `main.py` 的 `RealHardwareChecker`（目前 REAL 模式仍用 Mock 检查器）。
- 真实底盘执行器（`AutonomousState` 的 `controller`，目前是占位）。

**需要确认**：电机数量/类型、驱动板接口、编码器规格（PPR）、减速比、最大转速。

---

## 5. GPIO 引脚分配（P1）

**待定**：`config.py` 的 `Pin` 全是占位值（BUTTON=17 / LED_GREEN=22 / LED_RED=27 / BUZZER=18）。

**阻塞的软件**：真实 `GPIOButton` / `LEDIndicator` / 电机/舵机的引脚映射。

**需要确认**：一键启动按钮、绿/红 LED、蜂鸣器、电机/套取机构引脚的最终 BCM 编号。

---

## 6. 通信方案选型（P1）

**待定**：WiFi AP / 蓝牙 / Zigbee？

**阻塞的软件**：`communication/comm_server.py` 的 `WebSocketServer` 目前是占位，需按最终方案实现真实传输层。

**需要确认**：无线方案、调试模式（双向）与运行模式（仅广播）的带宽/延迟要求。

---

## 7. 电池规格确认（P2）

**待定**：3S LiPo 的确切电压范围、容量、放电能力。

**阻塞的软件**：`Thresholds.BATTERY_MIN/MAX_VOLTAGE`（当前 11.0~12.6V）需按实际电池校准，否则自检会误判低电量。

---

## 8. 辅助传感器（P2）

**待定**：超声波（HC-SR04？）、温度/振动传感器的型号与数量。

**阻塞的软件**：`system_check` 的温度/振动检查项、`sensor_fusion` 的多模态融合。

---

## 建议的催办顺序

1. **先定转运机构**（P0，决定 transport 是否合规、是否要推翻重写）
2. **再定摄像头 + IMU + 电机**（P0，决定感知定位 + 底盘能否落地）
3. 然后 GPIO 引脚、通信方案
4. 最后电池、辅助传感器
