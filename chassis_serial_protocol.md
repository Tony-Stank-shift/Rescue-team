# 上下位机底盘通信约定（v1）

本文档规定上位机（RDK/电脑）与 STM32F407 底盘控制器之间的串口协议、单位、坐标系和异常处理方式。双方实现均以本文档为准。

> 当前状态：本文档中的 `VEL`、`ODOM`、里程计和通信看门狗是下一阶段正式接口，STM32 端尚未全部实现。现有 `RPM`、`TESTRPM`、`PWM`、`TESTPWM` 和 `TEL` 属于调试接口。

## 1. 固定硬件参数

| 参数 | 数值 | 说明 |
| --- | ---: | --- |
| 编码器每轮计数 | 68028 count/rev | 已包含电机减速比和四倍频，不再额外乘减速比 |
| 车轮直径 | 65 mm | 轮胎实际直径 |
| 车轮周长 | 204.2035 mm | `pi * 65` |
| 左右轮中心距 | 209 mm | 两个驱动轮接地点中心之间的距离 |
| 最大目标轮速 | 250 RPM | STM32 速度闭环的软件限值 |
| 电机测试电压 | 12 V | 当前速度参数和前馈参数的标定电压 |

换算关系：

```text
1 RPM = 3.403392 mm/s
250 RPM = 850.848 mm/s
```

## 2. 串口参数与帧格式

```text
波特率：115200
数据位：8
校验位：无
停止位：1
流控制：无
编码：ASCII
字段分隔：英文逗号 ,
帧结束：\r\n
```

每行只能包含一帧。命令名使用大写英文，数值字段不得包含单位字符。

示例：

```text
VEL,500,1000\r\n
```

## 3. 坐标、方向和单位

STM32 使用以机器人启动位置为原点的局部坐标系：

- `x`：车头正前方为正。
- `y`：车体左侧为正。
- `theta`：逆时针旋转为正，启动朝向为 `0 rad`。
- 左右轮向车头方向转动时，轮速和编码器累计值均为正。
- 线速度 `v > 0` 表示前进。
- 角速度 `w > 0` 表示向左转（逆时针）。

STM32 内部建议统一使用：

- 距离：`mm`
- 线速度：`mm/s`
- 角度：`rad`
- 角速度：`rad/s`
- 轮速：`RPM`
- 编码器：`count`

只有生成 `ODOM` 数据包时，才把距离和轮线速度从毫米单位转换为米单位。

## 4. 上位机发给 STM32

### 4.1 连通测试

```text
PING
```

STM32 回复：

```text
PONG
```

### 4.2 启动

```text
START
```

行为约定：

- 上电后 STM32 默认禁止运动，必须先收到一次 `START`。
- 第一次有效 `START` 将局部里程计和对外累计编码器清零，并进入可运行状态。
- 重复发送 `START` 只回复确认，不得再次清零，也不得打断正在执行的速度指令。
- `ESTOP` 锁定后，`START` 不能解除锁定。

回复：

```text
ACK,START
```

### 4.3 车体速度指令

```text
VEL,v_mm_s,w_mrad_s
```

| 字段 | 类型 | 单位 | 含义 |
| --- | --- | --- | --- |
| `v_mm_s` | 有符号整数 | mm/s | 车体前后线速度，正数前进 |
| `w_mrad_s` | 有符号整数 | mrad/s | 车体角速度，正数向左转；STM32 收到后除以 1000 得到 rad/s |

示例：

```text
VEL,500,0       # 以 500 mm/s 直行
VEL,300,1000    # 前进并以 1 rad/s 向左转
VEL,0,-1500     # 以 1.5 rad/s 原地向右转
VEL,0,0         # 正常停车
```

注：实际串口发送内容中不包含 `#` 后面的说明。

上位机正常运行时以 `50 Hz`（每 `20 ms` 一帧）持续发送 `VEL`。STM32 不对每个 `VEL` 回复 `ACK`，避免串口积压。

STM32 按下式进行差速换算：

```text
w_rad_s = w_mrad_s / 1000
left_mm_s  = v_mm_s - w_rad_s * 209 / 2
right_mm_s = v_mm_s + w_rad_s * 209 / 2

left_rpm  = left_mm_s  / 3.403392
right_rpm = right_mm_s / 3.403392
```

如果任意一侧超过 `250 RPM`，必须将左右轮目标按相同比例一起缩小，使较大的一侧恰好等于 `250 RPM`。不能分别独立裁剪，否则会改变转弯曲率。速度越界只做限幅，不返回错误。

### 4.4 普通停车

```text
STOP
```

- 立即把左右轮目标速度设为零并清除 PI 积分。
- 保持串口接收和可运行状态，之后收到合法 `VEL` 可以继续行驶。
- 不清零里程计。

回复：

```text
ACK,STOP
```

### 4.5 紧急停车

```text
ESTOP
```

- 立即停止并进入紧急锁定状态。
- 锁定后拒绝所有运动命令。
- 本版不提供串口解锁命令，只能人工确认安全后复位 STM32。

回复：

```text
ACK,ESTOP
```

## 5. STM32 回传给上位机

### 5.1 轮式里程计

```text
ODOM,x_m,y_m,theta_rad,encL,encR,vL_m_s,vR_m_s
```

建议以 `20 Hz`（每 `50 ms` 一帧）回传。

| 字段 | 类型 | 单位 | 含义 |
| --- | --- | --- | --- |
| `x_m` | 小数 | m | 相对启动点的前向坐标 |
| `y_m` | 小数 | m | 相对启动点的左向坐标 |
| `theta_rad` | 小数 | rad | 相对启动朝向，逆时针为正，归一化到 `[-pi, pi]` |
| `encL` | 有符号整数 | count | 第一次 `START` 后左轮累计编码器计数，前进为正 |
| `encR` | 有符号整数 | count | 第一次 `START` 后右轮累计编码器计数，前进为正 |
| `vL_m_s` | 小数 | m/s | 左轮当前线速度，前进为正 |
| `vR_m_s` | 小数 | m/s | 右轮当前线速度，前进为正 |

示例：

```text
ODOM,0.523100,0.012400,0.083000,174321,180745,0.401200,0.419800
```

上位机必须只把前缀严格等于 `ODOM` 且字段数量、数值均合法的行当作里程计。`TEL`、`ACK`、`EVENT`、`ERR` 和乱码都必须忽略或分类记录，不能误解析为位姿。

里程计计算使用每个采样周期的编码器增量：

```text
dL_mm = deltaL / 68028 * pi * 65
dR_mm = deltaR / 68028 * pi * 65
ds = (dL_mm + dR_mm) / 2
dtheta = (dR_mm - dL_mm) / 209

x += ds * cos(theta + dtheta / 2)
y += ds * sin(theta + dtheta / 2)
theta = normalize(theta + dtheta)
```

### 5.2 事件

```text
EVENT,WATCHDOG_STOP
EVENT,TEST_DONE
```

- `WATCHDOG_STOP`：通信持续失联，STM32 已完成平滑停车。每次失联过程只发送一次。
- `TEST_DONE`：定时调试指令执行完毕并已停车。

### 5.3 错误

```text
ERR,FORMAT
ERR,TOO_LONG
ERR,OVERFLOW
ERR,UNKNOWN
ERR,NOT_STARTED
ERR,LOCKED
ERR,DURATION
```

错误含义：

| 错误 | 含义 |
| --- | --- |
| `FORMAT` | 字段数量错误或数值无法解析 |
| `TOO_LONG` | 单帧超过接收长度限制 |
| `OVERFLOW` | 串口接收环形缓冲区溢出 |
| `UNKNOWN` | 未知命令 |
| `NOT_STARTED` | 尚未执行 `START` 就发送运动命令 |
| `LOCKED` | 已进入紧急停车锁定状态 |
| `DURATION` | 测试时间无效或超过上限 |

## 6. 通信异常与自动恢复

可靠性原则是：单个坏包不停车，短暂断流不中断，持续失联平滑停车，通信恢复后自动继续。

| 距离最后一个合法 `VEL` 的时间 | STM32 行为 |
| ---: | --- |
| `0～300 ms` | 保持最后一个合法速度指令 |
| `300～800 ms` | 将最后速度按时间线性平滑降到零 |
| `>= 800 ms` | 保持目标速度为零，并发送一次 `EVENT,WATCHDOG_STOP` |

恢复规则：

- 格式错误、乱码、未知命令和接收溢出均不得刷新 `VEL` 看门狗。
- 坏包只被丢弃并记录错误，不立即停车；在超时阈值内继续执行最后一个合法速度。
- STM32 串口底层发生可恢复错误时，应自动重新启动接收。
- 失联后连续收到 `3` 个合法 `VEL`（相邻间隔不超过 `100 ms`）即自动恢复，并执行最新一帧，无需重新发送 `START`。
- 自动恢复只适用于普通通信失联，不适用于 `ESTOP`。

## 7. 当前调试接口

下列命令仅用于架空测试、速度环标定和排查接线，不作为上位机比赛控制接口：

```text
PWM,left_percent,right_percent
TESTPWM,left_percent,right_percent,duration_ms
RPM,left_rpm,right_rpm
TESTRPM,left_rpm,right_rpm,duration_ms
```

约束：

- PWM 自动限幅到 `-100～100 %`。
- RPM 自动限幅到 `-250～250 RPM`。
- `duration_ms` 范围为 `1～5000 ms`。
- 所有运动命令必须在 `START` 后执行。

当前调试遥测：

```text
TEL,tick_ms,left_target_rpm,left_actual_rpm,right_target_rpm,right_actual_rpm,left_pwm,right_pwm
```

| 字段 | 单位 | 含义 |
| --- | --- | --- |
| `tick_ms` | ms | STM32 上电后的毫秒计时 |
| `left_target_rpm` | RPM | 左轮目标转速 |
| `left_actual_rpm` | RPM | 左轮滤波后的实测转速 |
| `right_target_rpm` | RPM | 右轮目标转速 |
| `right_actual_rpm` | RPM | 右轮滤波后的实测转速 |
| `left_pwm` | % | 左轮当前 PWM 指令 |
| `right_pwm` | % | 右轮当前 PWM 指令 |

比赛模式建议关闭 `TEL`，或把频率限制在 `5～10 Hz`。即使保留，上位机也必须按帧前缀分类解析。

## 8. 启动与运行顺序

```text
1. 上位机打开串口
2. 上位机发送 PING，确认收到 PONG
3. 操作员按下一键启动按钮
4. 上位机发送一次 START，确认收到 ACK,START
5. 上位机以 50 Hz 持续发送 VEL
6. STM32 以 20 Hz 持续发送 ODOM
7. 正常结束发送 STOP；紧急情况发送 ESTOP
```

## 9. v1 边界

- v1 使用 ASCII 文本，暂未加入序号和 CRC。解析必须严格，不能用错误帧刷新看门狗。
- 如果实车电机干扰下仍出现可观测乱码，双方应一起升级协议，增加序号和 CRC；不能只改一端。
- STM32 提供的是局部纯轮式里程计；传感器融合、地图坐标转换和出发区全局偏移由上位机负责。
- 所有速度闭环参数目前基于 `12 V`、轮子架空测试结果，落地带载后必须重新验证并视情况调参。
